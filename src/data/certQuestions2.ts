import type { CertQuestion } from './certQuestions'

export const certQuestions2: readonly CertQuestion[] = [

  // ── Data Modeling ──
  {
    id: 1,
    domain: "data-modeling",
    question: "A developer reviews an Entity attribute and sees the property 'Delete Rule' is greyed out / not configurable. What is the most likely reason?",
    options: [
      "The attribute is marked Mandatory = Yes",
      "The Entity has no defined indexes",
      "The Entity is set to Public = Yes",
      "The attribute is not a reference (foreign key) attribute",
    ],
    correctIndex: 3,
    explanation: "The Delete Rule property only applies to reference (foreign key) attributes, so it is greyed out on plain data attributes like Text or Integer. Public, Mandatory, and indexes do not control the Delete Rule.",
  },
  {
    id: 2,
    domain: "data-modeling",
    question: "An Order entity references both Customer (Delete Rule = Protect) and Promotion (Delete Rule = Ignore). A developer deletes a Promotion that is used by existing Orders. What happens to those Orders?",
    options: [
      "The delete is blocked because the Orders still reference the Promotion",
      "The Promotion reference on each Order is automatically set to its default value",
      "The Orders remain but their Promotion reference now points to a non-existent record",
      "The Orders are deleted along with the Promotion",
    ],
    correctIndex: 2,
    explanation: "Delete Rule = Ignore lets the referenced (Promotion) record be deleted without affecting child Orders, leaving the Promotion reference pointing to a non-existent record. Protect would block the delete; Delete would cascade.",
  },
  {
    id: 3,
    domain: "data-modeling",
    question: "A developer wants a composite Unique Index on an Entity so that the combination of ProjectId and TaskCode is unique, even though each value can repeat individually. How is this configured?",
    options: [
      "Create two separate Unique Indexes, one per attribute",
      "Mark both attributes as Mandatory = Yes",
      "Create one Index containing both attributes and set it to Unique",
      "Set the Delete Rule of both references to Protect",
    ],
    correctIndex: 2,
    explanation: "A single Index containing both attributes with Unique = Yes enforces uniqueness on the combination while each value can repeat individually. Two separate unique indexes would forbid repeats of either value alone.",
  },
  {
    id: 4,
    domain: "data-modeling",
    question: "In Service Studio, where are an Entity's auto-generated CRUD actions (CreateOrUpdate, Get, Delete, etc.) accessed from?",
    options: [
      "In the Interface tab under the screen's actions",
      "Only inside an Aggregate's data source list",
      "Under the Entity itself in the Data tab, expanded as Entity Actions",
      "In the Logic tab under Server Actions",
    ],
    correctIndex: 2,
    explanation: "Entity Actions (CreateOrUpdate, Get, Delete, etc.) are auto-generated and accessed directly under the Entity in the Data tab. They are not Server Actions in the Logic tab nor limited to Aggregates.",
  },
  {
    id: 5,
    domain: "data-modeling",
    question: "A developer needs to store a yes/no flag (IsActive) on an Entity. Which attribute Data Type is the correct, type-safe choice in OutSystems?",
    options: [
      "Boolean",
      "Static Entity reference with two records",
      "Text with values 'Y' and 'N'",
      "Integer with values 0 and 1",
    ],
    correctIndex: 0,
    explanation: "OutSystems has a native Boolean data type for true/false flags, which is the type-safe choice. Integer/Text representations lose type safety and a Static Entity is unnecessary overhead.",
  },
  {
    id: 6,
    domain: "data-modeling",
    question: "A developer changes the Data Type of an existing Entity attribute from Integer to Text and publishes. What does the platform do with the existing stored data?",
    options: [
      "It blocks publish entirely and requires a new attribute instead",
      "It attempts to convert and migrate the existing values to the new type during publish",
      "It deletes all existing records to avoid conversion errors",
      "It leaves the database column unchanged and only updates new records",
    ],
    correctIndex: 1,
    explanation: "On publish OutSystems performs a database migration to convert the column and carry over compatible existing values (Integer to Text is a safe/widening conversion). It does not delete records or refuse compatible changes.",
  },
  {
    id: 7,
    domain: "data-modeling",
    question: "What is the effect of setting an Entity's 'Is Public' property to No (the default)?",
    options: [
      "The Entity's records are encrypted at rest",
      "Other modules cannot reference the Entity, but it is fully usable within its own module",
      "The Entity cannot be queried by Aggregates anywhere",
      "The Entity becomes a Static Entity",
    ],
    correctIndex: 1,
    explanation: "A non-public (Private) Entity is only visible within its own module; it works normally there but cannot be referenced by other modules. Visibility is unrelated to Static type or encryption.",
  },
  {
    id: 8,
    domain: "data-modeling",
    question: "A developer wants to delete the auto-generated 'Id' Identifier attribute of an Entity to use a custom text key instead. What does Service Studio allow?",
    options: [
      "The Id attribute can be deleted like any other attribute",
      "Identifiers can only be changed in the database, not in Service Studio",
      "The Id attribute cannot be deleted, but the developer can change which attribute is the Identifier",
      "The Id is automatically removed once a Unique Index is added",
    ],
    correctIndex: 2,
    explanation: "The auto-generated Id Identifier cannot be deleted, but you can designate a different attribute as the Entity's Identifier in its properties. Adding a unique index does not remove the Id.",
  },
  {
    id: 9,
    domain: "data-modeling",
    question: "A developer adds a reference attribute named 'Manager' on the Employee entity that points back to Employee (a self-reference). Is this valid in OutSystems?",
    options: [
      "Yes, self-references are allowed and useful for hierarchies",
      "No, an Entity can never reference itself",
      "Only Static Entities can self-reference",
      "Only if the Delete Rule is set to Delete",
    ],
    correctIndex: 0,
    explanation: "Self-referencing Entities are fully supported and commonly used to model hierarchies such as org charts. There is no requirement for Static type or a specific Delete Rule.",
  },
  {
    id: 10,
    domain: "data-modeling",
    question: "Which statement correctly distinguishes a Mandatory attribute from a Unique-Indexed attribute?",
    options: [
      "Neither has any effect on stored data",
      "Both forbid nulls and duplicates identically",
      "Mandatory forbids null values; a Unique Index forbids duplicate values",
      "Mandatory forbids duplicates; Unique Index forbids nulls",
    ],
    correctIndex: 2,
    explanation: "Mandatory prevents null/empty values while a Unique Index prevents duplicate values; they enforce different constraints and are often combined.",
  },
  {
    id: 11,
    domain: "data-modeling",
    question: "A Static Entity 'Country' is referenced by the regular Entity 'Address'. The developer tries to delete a Country record at design time that an Address record references. Considering Static Entity records exist only at design time, what governs this relationship?",
    options: [
      "The Address reference is automatically nullified at publish",
      "A runtime Delete cascade removes the Address",
      "The reference still has a Delete Rule that controls referential integrity for the underlying record",
      "Static Entities ignore all referential integrity",
    ],
    correctIndex: 2,
    explanation: "A reference to a Static Entity still carries a Delete Rule (default Protect) enforcing referential integrity at the database level. Static type does not disable referential rules.",
  },
  {
    id: 12,
    domain: "data-modeling",
    question: "A developer wants to bootstrap a regular (non-static) Entity with initial seed records on first publish. Which approach is the standard OutSystems pattern?",
    options: [
      "Convert the Entity to Public so seed data appears automatically",
      "Use a Timer or bootstrap logic that runs CreateOrUpdate actions on the Entity",
      "Define records in the Entity's design-time records table like a Static Entity",
      "Set the Entity's 'Bootstrap' property to Yes",
    ],
    correctIndex: 1,
    explanation: "Regular Entities have no design-time records table, so seeding uses bootstrap logic (often a Timer) calling Create/CreateOrUpdate actions. There is no 'Bootstrap' property and only Static Entities hold design-time records.",
  },
  {
    id: 13,
    domain: "data-modeling",
    question: "In an Entity Diagram, a developer drags from one Entity to another. What does this gesture create?",
    options: [
      "A new Aggregate joining the two Entities",
      "A bidirectional synchronization between the Entities",
      "A new reference (foreign key) attribute on the source Entity",
      "A Unique Index spanning both Entities",
    ],
    correctIndex: 2,
    explanation: "Drawing a connection between Entities in an Entity Diagram creates a reference (foreign key) attribute on the source Entity pointing to the target. It does not create Aggregates, indexes, or sync.",
  },
  {
    id: 14,
    domain: "data-modeling",
    question: "A developer needs to store a large block of binary data (an uploaded file's content) on an Entity. Which attribute Data Type is appropriate?",
    options: [
      "Email",
      "Record",
      "Binary Data",
      "Text",
    ],
    correctIndex: 2,
    explanation: "The Binary Data type stores raw binary content such as uploaded file content or images. Text is for character data; Record/Email are unsuitable for binary.",
  },
  {
    id: 15,
    domain: "data-modeling",
    question: "What is the consequence of leaving a reference attribute's 'Mandatory' property set to No (optional)?",
    options: [
      "Other modules cannot read the attribute",
      "No index is created on the foreign key",
      "The Delete Rule is forced to Ignore",
      "The reference can be null, meaning a record may exist without pointing to a parent",
    ],
    correctIndex: 3,
    explanation: "A non-mandatory reference may be null, so a child record can exist without pointing to a parent. Mandatory-ness is independent of the Delete Rule, index creation, and visibility.",
  },
  {
    id: 16,
    domain: "data-modeling",
    question: "A developer creates an Index but does NOT check the 'Unique' option. What does this non-unique index accomplish?",
    options: [
      "It enforces that values cannot repeat",
      "It speeds up filtering and sorting on the indexed attribute(s) without restricting duplicates",
      "It makes the attribute mandatory",
      "It exposes the attribute to other modules",
    ],
    correctIndex: 1,
    explanation: "A non-unique index improves query performance for filters and sorts while still allowing duplicate values. Only the Unique option forbids duplicates; indexes do not affect Mandatory or visibility.",
  },
  {
    id: 17,
    domain: "data-modeling",
    question: "Which OutSystems data type should be used for an Entity attribute that stores only a calendar date with no time component?",
    options: [
      "Date Time",
      "Time",
      "Date",
      "Text",
    ],
    correctIndex: 2,
    explanation: "The Date data type stores a calendar date with no time component. Date Time includes both, and Time stores only time-of-day.",
  },
  {
    id: 18,
    domain: "data-modeling",
    question: "A developer needs to model that one Department 'has many' Employees. Where is the reference attribute placed to correctly express this one-to-many relationship?",
    options: [
      "A list of Employee references is added on the Department entity",
      "References are added on both entities pointing to each other",
      "A junction entity is required for one-to-many relationships",
      "A DepartmentId reference attribute is added on the Employee (the 'many' side) entity",
    ],
    correctIndex: 3,
    explanation: "In a one-to-many relationship the foreign key lives on the 'many' side, so Employee gets a DepartmentId reference. Attributes cannot hold lists, and junction entities are only for many-to-many.",
  },
  {
    id: 19,
    domain: "data-modeling",
    question: "A developer publishes a module and the platform reports that a Unique Index could not be created because of existing duplicate values. What is the correct interpretation?",
    options: [
      "The duplicates are kept and the newest record wins",
      "The index was created but marked non-unique automatically",
      "OutSystems automatically removed the duplicates and created the index",
      "The publish/index creation is blocked until the duplicate data is resolved",
    ],
    correctIndex: 3,
    explanation: "A Unique Index cannot be created over data containing duplicates, so the operation/publish is blocked until the duplicate records are resolved. The platform does not silently delete data or downgrade the index.",
  },
  {
    id: 20,
    domain: "data-modeling",
    question: "What does the auto-created '<Entity>Id' Identifier type (e.g., CustomerId) provide compared to using a plain Integer for the same value?",
    options: [
      "It allows the value to be null by default",
      "It is a distinct, strongly-typed identifier that prevents accidentally assigning an unrelated Id",
      "It automatically encrypts the value",
      "It makes the attribute mandatory in every screen",
    ],
    correctIndex: 1,
    explanation: "Each Entity has its own strongly-typed Identifier (e.g., CustomerId) so the platform prevents assigning an unrelated Id type. A plain Integer offers no such type safety.",
  },
  {
    id: 21,
    domain: "data-modeling",
    question: "A developer wants an attribute's value to be required in the database but is fine with the platform showing a validation message at the screen instead of a hard DB error. What does setting Mandatory = Yes on the attribute primarily do?",
    options: [
      "It defines the attribute as not-null at the data layer and drives platform validation on save operations",
      "It creates a Unique Index automatically",
      "It only adds client-side JavaScript validation",
      "It changes the Delete Rule to Protect",
    ],
    correctIndex: 0,
    explanation: "Mandatory = Yes marks the attribute as required (not-null) at the data layer and drives platform validation on save operations. It does not create indexes or change Delete Rules.",
  },
  {
    id: 22,
    domain: "data-modeling",
    question: "A developer adds a new attribute to a published Entity and publishes again. What is the value of this new attribute for all the pre-existing records?",
    options: [
      "The records are deleted because they lack the new attribute",
      "Each pre-existing record gets the attribute's default value for its data type (e.g., 0, empty text, or null reference)",
      "Publish fails until each record is updated manually",
      "The new attribute is only available to records created after publish",
    ],
    correctIndex: 1,
    explanation: "Adding an attribute fills existing records with the data type's default value (0 for Integer, empty Text, null reference) without deleting them or failing publish; the attribute applies to all records.",
  },

  // ── Aggregates & Fetching ──
  {
    id: 23,
    domain: "aggregates",
    question: "A developer adds two Sorts to an Aggregate: first 'Customer.Country Ascending', then 'Order.Total Descending'. How does the Aggregate order the result set?",
    options: [
      "By Country ascending; within each Country, by Total descending",
      "By Total descending only; the Country sort is ignored",
      "By Total descending; within each Total, by Country ascending",
      "By whichever sort the user clicks at runtime",
    ],
    correctIndex: 0,
    explanation: "Aggregate Sorts apply in listed order: the first sort is primary, the second breaks ties within it. So records order by Country ascending, then Total descending within each Country.",
  },
  {
    id: 24,
    domain: "aggregates",
    question: "In an Aggregate that joins Order 'With or Without' Shipment, a calculated attribute reads Shipment.TrackingCode. For an Order that has no matching Shipment, what value does that attribute hold in the returned row?",
    options: [
      "The previous row's TrackingCode",
      "NullTextIdentifier of the Order entity",
      "The row is removed from the result set entirely",
      "Empty/NULL because the outer join produced no Shipment match",
    ],
    correctIndex: 3,
    explanation: "A 'With or Without' (left outer) join keeps the Order even without a Shipment match, and unmatched joined-entity attributes return as their type's empty/NULL value; the row is not dropped.",
  },
  {
    id: 25,
    domain: "aggregates",
    question: "A developer wants an Aggregate to return only unique Customer Country values with no duplicates. Which Aggregate feature produces this?",
    options: [
      "Group By Country so each distinct Country appears once",
      "Set Max Records to 1",
      "Add a Filter that removes duplicates",
      "Sort by Country Ascending",
    ],
    correctIndex: 0,
    explanation: "Grouping by Country collapses records so each distinct Country appears once, producing distinct values. Sorting only orders rows, a filter cannot deduplicate, and Max Records just limits count.",
  },
  {
    id: 26,
    domain: "aggregates",
    question: "An Aggregate groups Orders by Customer and adds a Count. A developer references this Count in the result. What does the Count column represent?",
    options: [
      "The value of the Max Records property",
      "The number of Order records in each Customer's group",
      "The number of distinct Customers in the Aggregate",
      "The total Order records across all groups combined",
    ],
    correctIndex: 1,
    explanation: "Count on a grouped Aggregate computes records within each group, returning Orders per Customer. It is a per-group aggregate, not a global total.",
  },
  {
    id: 27,
    domain: "aggregates",
    question: "A developer sets an Aggregate's Max Records property to a value bound to a Local Variable. What is the effect when that Local Variable changes at runtime?",
    options: [
      "The screen throws a runtime error",
      "The Aggregate re-fetches with the new limit because Max Records is a reactive dependency",
      "Only the OnAfterFetch re-runs, not the query",
      "Max Records is fixed at design time and ignores the variable",
    ],
    correctIndex: 1,
    explanation: "Max Records can be bound to a variable/expression, and because the Aggregate reactively tracks its inputs, changing that variable triggers a re-fetch with the new limit.",
  },
  {
    id: 28,
    domain: "aggregates",
    question: "An Aggregate filter is written as 'Product.Price >= MinPrice and Product.Price <= MaxPrice'. A developer instead adds this as TWO separate filters. What is the functional result?",
    options: [
      "Identical results, since separate filters are AND-combined",
      "The two filters are OR-combined, widening the results",
      "An error, because a filter cannot reference the same attribute twice",
      "Only the second filter is evaluated",
    ],
    correctIndex: 0,
    explanation: "Separate Aggregate filters are AND-combined, so '>= MinPrice' and '<= MaxPrice' as two filters yield the same range as the single combined expression.",
  },
  {
    id: 29,
    domain: "aggregates",
    question: "A developer needs an Aggregate on Screen A to be re-executed after a Save action elsewhere on the same screen completes. What is the correct way to trigger it?",
    options: [
      "Re-publish the module so the cache clears",
      "Call the generated Refresh<AggregateName> action after the Save",
      "Reassign the Aggregate's .List to itself",
      "Set the Aggregate's Fetch property to 'Only On Demand'",
    ],
    correctIndex: 1,
    explanation: "Each Aggregate generates a Refresh action (e.g., RefreshGetOrders) that re-runs the query with current inputs; call it after the Save to update results.",
  },
  {
    id: 30,
    domain: "aggregates",
    question: "An Aggregate joins Customer 'With' Address (Only With). A developer expects every Customer but sees Customers without an Address dropped. Which single change keeps ALL Customers regardless of Address?",
    options: [
      "Add a filter 'Address.Id <> NullIdentifier()'",
      "Increase Max Records",
      "Change the join to 'With or Without'",
      "Add a Sort on Address",
    ],
    correctIndex: 2,
    explanation: "'Only With' is an inner join excluding Customers lacking an Address; switching to 'With or Without' (left outer) returns all Customers with empty Address attributes when unmatched.",
  },
  {
    id: 31,
    domain: "aggregates",
    question: "In an Aggregate's OnAfterFetch handler, a developer wants to read the fetched records. What is available to the handler?",
    options: [
      "The Aggregate's result list, available client-side for post-processing",
      "Nothing; OnAfterFetch cannot see the fetched data",
      "Only the .Count value, not the list",
      "A writable cursor that updates the database as you loop",
    ],
    correctIndex: 0,
    explanation: "OnAfterFetch runs client-side after fetch and can access the Aggregate's result list for post-processing; it does not write to the database.",
  },
  {
    id: 32,
    domain: "aggregates",
    question: "A developer wants to know how many records were actually fetched and rendered (after Max Records), to show 'Showing N rows'. Which expression returns that fetched count?",
    options: [
      "GetData.Count",
      "GetData.List.Count",
      "GetData.MaxRecords",
      "GetData.List.Length",
    ],
    correctIndex: 1,
    explanation: "For an Aggregate, GetData.List.Count returns the number of records actually fetched into the list (capped by Max Records). The Aggregate's own .Count output property returns the total matching records ignoring Max Records. List structures expose .Length but the Aggregate output uses .List.Count.",
  },
  {
    id: 33,
    domain: "aggregates",
    question: "A developer adds a filter that references a Session Variable in an Aggregate. The Session Variable is changed in a later Client Action without touching the screen's Aggregate. What happens to the Aggregate?",
    options: [
      "It re-runs on the next full page reload only",
      "Filters cannot reference Session Variables at all",
      "It immediately re-queries because Session Variables are always reactive",
      "It re-runs only if you explicitly call its Refresh action",
    ],
    correctIndex: 3,
    explanation: "Aggregates reactively re-fetch when bound local screen variables/inputs change, but a Session Variable changed elsewhere is not tracked as a screen dependency, so you must call Refresh to re-query.",
  },
  {
    id: 34,
    domain: "aggregates",
    question: "Two independent Aggregates on a screen both have Fetch = 'At Start'. How does the platform handle their initial fetching?",
    options: [
      "Only the first Aggregate fetches; the second waits for a Refresh call",
      "They are fetched asynchronously and can run in parallel, each rendering when ready",
      "They fetch sequentially, the second waiting for the first to finish",
      "They are merged into a single combined query automatically",
    ],
    correctIndex: 1,
    explanation: "Independent screen Aggregates fetch asynchronously and can run in parallel, with the screen rendering progressively as each one's data arrives.",
  },
  {
    id: 35,
    domain: "aggregates",
    question: "A developer creates a calculated attribute in an Aggregate as 'If(Order.Total > 1000, \"VIP\", \"Standard\")'. What is true about this output column?",
    options: [
      "It mutates the Order entity in the database",
      "It can only be referenced inside OnAfterFetch",
      "It is computed server-side and returned as part of each record",
      "It is recomputed on the client for every render",
    ],
    correctIndex: 2,
    explanation: "Aggregate calculated/output columns are compiled into the server query and returned with each record. They do not mutate the entity and are not limited to OnAfterFetch.",
  },
  {
    id: 36,
    domain: "aggregates",
    question: "A developer needs an Aggregate that returns Orders placed in a date range chosen by two date pickers. To make the preview show results at design time without affecting runtime, what should be supplied?",
    options: [
      "Test Values for the date variables on the Test Values tab",
      "Hardcoded dates inside the filter expression",
      "Default values on the date Local Variables stored to the database",
      "Site Properties holding the date range",
    ],
    correctIndex: 0,
    explanation: "The Test Values tab supplies sample values for the variables a filter references so the design-time preview returns rows, with no runtime effect.",
  },
  {
    id: 37,
    domain: "aggregates",
    question: "An Aggregate has Fetch = 'At Start' and a filter referencing input parameter SearchTerm. The user types into the bound input. What re-runs the query?",
    options: [
      "Nothing until the screen is reopened",
      "A manual page refresh in the browser",
      "Only the OnAfterFetch event, not the query itself",
      "Automatically, because the Aggregate reactively depends on the changed variable",
    ],
    correctIndex: 3,
    explanation: "Reactive Aggregates track the variables in their definition; when the bound search variable changes, the Aggregate automatically re-fetches. OnAfterFetch cannot re-query and no manual reload is needed.",
  },
  {
    id: 38,
    domain: "aggregates",
    question: "In an Aggregate joining three entities, a developer adds a filter 'OrderItem.Quantity > 0 or Order.IsPriority'. How is this single filter evaluated relative to other filters in the same Aggregate?",
    options: [
      "The OR is ignored because Aggregates only support AND",
      "The OR is split into two AND filters automatically",
      "Only the first operand of the OR is used",
      "This whole expression is one condition, AND-combined with any other filters",
    ],
    correctIndex: 3,
    explanation: "Within a single filter you can use 'or' freely; that whole expression is one boolean condition, and multiple separate filters are then AND-combined. The platform does not strip or split the OR.",
  },
  {
    id: 39,
    domain: "aggregates",
    question: "A developer wants the total revenue across ALL Orders shown as a single number. Which Aggregate configuration produces one summary value?",
    options: [
      "Use OnAfterFetch to add up .List in a loop",
      "Set Max Records to 1 and read Order.Total",
      "Read GetOrders.Count",
      "Add a Sum on Order.Total with no Group By, yielding a single grouped result",
    ],
    correctIndex: 3,
    explanation: "Adding a Sum aggregate function with no Group By collapses the result to one row holding the overall total, computed server-side. .Count only counts records and Max Records=1 just truncates.",
  },
  {
    id: 40,
    domain: "aggregates",
    question: "A developer references GetCustomers.List.Empty in an If to show a 'No results' message. What does .Empty evaluate to when the Aggregate fetched zero matching records?",
    options: [
      "An empty string",
      "True",
      "False",
      "It throws because .Empty is not a list property",
    ],
    correctIndex: 1,
    explanation: "The .Empty property of a List returns True when the list has no records, making it the natural condition for a 'No results' message; it is a Boolean property.",
  },
  {
    id: 41,
    domain: "aggregates",
    question: "An Aggregate's Fetch is set to 'Only On Demand' and bound to a Table widget. Before any Refresh is called, what does the Table display?",
    options: [
      "A runtime null-list error",
      "No rows, because .List is empty until the Refresh runs",
      "The Test Values rows",
      "A loading spinner indefinitely",
    ],
    correctIndex: 1,
    explanation: "With Fetch = 'Only On Demand', the query does not run at start, so .List is empty and the Table shows no rows until Refresh executes. No error is thrown and Test Values never affect runtime.",
  },
  {
    id: 42,
    domain: "aggregates",
    question: "A developer joins Order and Product through OrderItem in one Aggregate. Where is this multi-entity join resolved?",
    options: [
      "On the client, by combining three separate fetches",
      "In a compiled single SQL query executed on the server",
      "Inside the OnAfterFetch handler",
      "In the browser's Local Storage cache",
    ],
    correctIndex: 1,
    explanation: "The visual Aggregate, including multi-entity joins, is compiled into one optimized SQL query executed on the database server; the joined result is returned to the client.",
  },
  {
    id: 43,
    domain: "aggregates",
    question: "A developer wants results sorted by a calculated attribute (output column) defined in the same Aggregate. Is this possible?",
    options: [
      "No; calculated attributes are computed only after sorting",
      "Only if the calculated attribute is also added to a Group By",
      "No; sorts can only use stored entity attributes",
      "Yes; the Aggregate can sort by its calculated attributes as well as entity attributes",
    ],
    correctIndex: 3,
    explanation: "Aggregates allow sorting on calculated/output columns as well as entity attributes, with ordering applied server-side; a Group By is not required.",
  },
  {
    id: 44,
    domain: "aggregates",
    question: "A developer notices a screen Aggregate is fetched once and its results are reused as the user interacts, without re-hitting the server each render. What describes this behavior?",
    options: [
      "Aggregates re-query the database on every UI render",
      "The fetched result set is cached on the client until inputs change or Refresh is called",
      "Results are stored permanently in a Static Entity",
      "The data is held only in a Session Variable on the server",
    ],
    correctIndex: 1,
    explanation: "After fetching, the Aggregate's results are cached client-side and reused across renders; a new server query happens only when a tracked input changes or Refresh is invoked.",
  },
  {
    id: 45,
    domain: "aggregates",
    question: "A developer needs the most recent 5 Orders for a Customer. Which combination of Aggregate settings achieves exactly this?",
    options: [
      "Set Max Records to 5 only, no sort or filter",
      "Filter by Customer, Group By Order.Date, Count = 5",
      "Sort by Order.Date Ascending and set Max Records to 5",
      "Sort by Order.Date Descending and set Max Records to 5, with a Customer filter",
    ],
    correctIndex: 3,
    explanation: "Filtering to the Customer, sorting Date Descending (newest first), and limiting Max Records to 5 returns the 5 most recent Orders. Ascending gives the oldest 5; Max Records alone gives an arbitrary set.",
  },

  // ── Screen Lifecycle ──
  {
    id: 46,
    domain: "screen-lifecycle",
    question: "A developer adds an Aggregate to a Reactive screen and writes logic in OnInitialize that reads the Aggregate's .List.Length to decide whether to redirect. At runtime .List.Length is always 0 even though records exist. What explains this?",
    options: [
      "The Aggregate's Fetch property must be set to 'Only on demand' to expose .List in OnInitialize",
      "Aggregates never populate .List on the client and must be read via a Server Action",
      "OnInitialize runs before the Aggregate's asynchronous fetch completes, so the data is not yet available there",
      "OnInitialize cannot access Aggregate properties at all and throws silently",
    ],
    correctIndex: 2,
    explanation: "OnInitialize executes before the screen's Aggregate/Data Action fetches complete, so .List.Length is still 0 there; logic depending on fetched data belongs in OnAfterFetch. Aggregates do populate .List on the client once the fetch completes.",
  },
  {
    id: 47,
    domain: "screen-lifecycle",
    question: "On a Reactive screen, an Aggregate's OnAfterFetch handler runs but the developer notices it fires again after they click a button that calls Refresh Data on that Aggregate. Is this expected?",
    options: [
      "No, Refresh Data triggers OnParametersChanged instead",
      "Yes, but only if the Aggregate returned different records than before",
      "No, OnAfterFetch fires only once on the initial screen load",
      "Yes, OnAfterFetch fires after every completed fetch, including each Refresh Data",
    ],
    correctIndex: 3,
    explanation: "OnAfterFetch fires after every completed fetch of the Aggregate/Data Action, including the initial load and each Refresh Data, regardless of whether the result set changed.",
  },
  {
    id: 48,
    domain: "screen-lifecycle",
    question: "A Block has two Input Parameters, ProductId and CategoryId. The parent changes only CategoryId at runtime. What happens to the Block's OnParametersChanged event?",
    options: [
      "It fires once because at least one input parameter changed value",
      "It fires once for each changed input, so it runs twice",
      "It does not fire because ProductId did not change",
      "It fires only if both input parameters change in the same cycle",
    ],
    correctIndex: 0,
    explanation: "OnParametersChanged fires a single time per change cycle when one or more input parameters change value; it is not per-parameter and does not require all inputs to change.",
  },
  {
    id: 49,
    domain: "screen-lifecycle",
    question: "A developer wants a Block to receive the screen's currently selected record and re-run an internal Aggregate whenever that record changes. Which design correctly causes the re-fetch?",
    options: [
      "Define an Output Parameter on the Block that the parent updates",
      "Pass the record via an Input Parameter and reference that input in the Block Aggregate's filter",
      "Call the parent's Refresh Data action from the Block's OnRender",
      "Read the parent's Local Variable directly from inside the Block",
    ],
    correctIndex: 1,
    explanation: "Referencing a Block input parameter in the Aggregate's filter/definition creates a reactive dependency, so changing that input from the parent automatically re-fetches the Aggregate.",
  },
  {
    id: 50,
    domain: "screen-lifecycle",
    question: "In a Reactive screen, where does an Aggregate's OnAfterFetch event handler execute?",
    options: [
      "Half on the server and half on the client depending on the Fetch property",
      "On the server only when the Aggregate uses a SQL node",
      "On the client (browser), after the fetched data arrives",
      "On the server, immediately after the database query returns",
    ],
    correctIndex: 2,
    explanation: "OnAfterFetch is a client-side event handler running in the browser once fetched data arrives; the Aggregate query itself executes on the server.",
  },
  {
    id: 51,
    domain: "screen-lifecycle",
    question: "A developer sets a screen Aggregate's Fetch property to 'Only on demand'. When the screen first opens, what is the state of the Aggregate?",
    options: [
      "OnAfterFetch fires immediately with an empty result",
      "It throws a 'not fetched' runtime error when the screen renders",
      "Its data is empty and no fetch occurs until Refresh Data is called",
      "It fetches automatically anyway because screens always load their Aggregates",
    ],
    correctIndex: 2,
    explanation: "With Fetch = 'Only on demand', no fetch occurs at screen load; the Aggregate's data stays empty (no error) until a Refresh Data action runs it.",
  },
  {
    id: 52,
    domain: "screen-lifecycle",
    question: "A Reactive screen receives an Input Parameter named CustomerId. The developer needs the screen to be reachable by direct URL with that value. What must they ensure about CustomerId?",
    options: [
      "Input Parameters cannot appear in a screen's URL; use a Local Variable",
      "It must be a Session Variable so it survives the URL",
      "It must be marked as an Output Parameter to be passed in the URL",
      "It must be defined as a Screen Input Parameter; mandatory inputs without defaults require a value to navigate",
    ],
    correctIndex: 3,
    explanation: "Screen Input Parameters are passed during navigation and appear in the address; a mandatory input without a default value requires a value to navigate to the screen.",
  },
  {
    id: 53,
    domain: "screen-lifecycle",
    question: "A screen Aggregate filters on a Local Variable. A button action assigns a new value to that Local Variable but the data does not visibly change. What is the most likely cause?",
    options: [
      "Aggregates ignore Local Variables and only react to Input Parameters",
      "The new value assigned equals the previous value, so no re-fetch is triggered",
      "The Aggregate must be moved into the button's action flow",
      "Local Variable changes never trigger Aggregate re-fetches in Reactive",
    ],
    correctIndex: 1,
    explanation: "Reactive dependency tracking re-fetches only when a referenced value actually changes; assigning the same value yields no re-fetch. Local Variables used in filters do trigger re-fetches when their value differs.",
  },
  {
    id: 54,
    domain: "screen-lifecycle",
    question: "A developer needs to run logic right after a Data Action on the screen finishes loading its outputs. Which event handler should they attach the logic to?",
    options: [
      "The screen's OnRender",
      "The Data Action's OnAfterFetch handler",
      "The screen's OnInitialize",
      "The Data Action's OnParametersChanged",
    ],
    correctIndex: 1,
    explanation: "Screen-level Data Actions support an OnAfterFetch handler that runs once the fetch completes, the correct place for post-fetch logic; OnInitialize runs before the fetch completes.",
  },
  {
    id: 55,
    domain: "screen-lifecycle",
    question: "What is a key difference between a Screen's and a Block's lifecycle regarding the OnParametersChanged event?",
    options: [
      "Only Screens expose OnParametersChanged; Blocks use OnRender instead",
      "Both Screens and Blocks expose OnParametersChanged for their input parameters",
      "Only Blocks expose OnParametersChanged; a Screen reacts to input changes by re-navigating, not via OnParametersChanged",
      "Neither Screens nor Blocks have an OnParametersChanged event",
    ],
    correctIndex: 2,
    explanation: "OnParametersChanged is a Block event reacting to changes in the Block's input parameters; a Screen instead receives new input values through navigation rather than firing OnParametersChanged.",
  },
  {
    id: 56,
    domain: "screen-lifecycle",
    question: "A developer wants OnRender logic to run only on the very first render and skip later re-renders. What is the supported approach?",
    options: [
      "OnRender always runs once; no guard is needed",
      "Use a boolean Local Variable as a flag, set it after the first run, and guard the logic with an If",
      "Move the entire OnRender logic into OnAfterFetch automatically",
      "Set the OnRender event's 'Run Once' property to True",
    ],
    correctIndex: 1,
    explanation: "OnRender runs on the initial render and every re-render, so run-once logic is guarded with a boolean Local Variable flag set after the first run. There is no 'Run Once' property; one-time setup often belongs in OnInitialize.",
  },
  {
    id: 57,
    domain: "screen-lifecycle",
    question: "On screen load, in what relative order do OnInitialize, the Aggregate fetch, and OnRender occur for the first paint?",
    options: [
      "OnRender first, then OnInitialize, then the Aggregate fetch",
      "OnInitialize first, then the Aggregate fetch (firing OnAfterFetch), with OnRender painting the UI",
      "Aggregate fetch first, then OnRender, then OnInitialize",
      "OnAfterFetch first, then OnInitialize, then OnRender",
    ],
    correctIndex: 1,
    explanation: "OnInitialize runs first, then Aggregates/Data Actions fetch (each firing OnAfterFetch on completion), with OnRender painting the UI as state changes. OnRender never precedes OnInitialize.",
  },
  {
    id: 58,
    domain: "screen-lifecycle",
    question: "A developer calls Refresh Data on Aggregate GetOrders inside a button's Client Action. What round-trip behavior occurs?",
    options: [
      "A server round-trip occurs to re-execute the Aggregate query and return fresh data",
      "No round-trip; Refresh Data only re-reads the client cache",
      "Refresh Data only works inside a Server Action, so it errors in a Client Action",
      "It re-runs OnInitialize instead of fetching data",
    ],
    correctIndex: 0,
    explanation: "Refresh Data re-executes the Aggregate on the server, so a round-trip occurs to obtain fresh data, after which OnAfterFetch fires. It is invoked from client-side logic, not restricted to Server Actions.",
  },
  {
    id: 59,
    domain: "screen-lifecycle",
    question: "A Detail screen passes a record Id to a child Block via an input. The Block's OnInitialize uses that input to set up a label, but on later input changes the label is stale. How should the developer fix it?",
    options: [
      "Convert the input into an Output Parameter",
      "Set the Block's Input Parameter to 'Refresh on change' in its properties",
      "Call the Block's OnInitialize manually from the parent",
      "Move the label-setup logic to the Block's OnParametersChanged so it re-runs when the input changes",
    ],
    correctIndex: 3,
    explanation: "OnInitialize runs only once when the Block is created and won't react to later input changes; logic responding to changing inputs belongs in OnParametersChanged. There is no 'Refresh on change' input property.",
  },
  {
    id: 60,
    domain: "screen-lifecycle",
    question: "Which statement about a screen's Input Parameters during OnInitialize is correct?",
    options: [
      "They already hold the values passed during navigation and can be used to filter Aggregates",
      "They can only be read inside OnAfterFetch handlers",
      "They are write-only and used to return values to the caller",
      "They are still null during OnInitialize and only populate at OnRender",
    ],
    correctIndex: 0,
    explanation: "By the time OnInitialize runs, the screen's Input Parameters already hold the values passed during navigation, so they are available to initialize logic and drive Aggregate filters.",
  },
  {
    id: 61,
    domain: "screen-lifecycle",
    question: "A developer needs reusable UI with its own data fetching, internal state, and the ability to notify the parent of user actions, embedded in several screens. Which element and mechanism are correct?",
    options: [
      "A Screen referenced inside other screens via its URL",
      "A Client Variable shared across screens",
      "A Static Entity exposing the shared UI",
      "A Block that defines Events handled by the parent screen",
    ],
    correctIndex: 3,
    explanation: "A Block is reusable embeddable UI that can contain its own Aggregates/state and communicates upward via Events the parent wires to handlers. A Screen has its own URL and is a navigation target, not embeddable UI.",
  },
  {
    id: 62,
    domain: "screen-lifecycle",
    question: "During an Aggregate's asynchronous fetch, a developer binds a Table to that Aggregate's List. What does the user see while the fetch is in progress?",
    options: [
      "The previous screen's data until the fetch completes",
      "A runtime error because the List is null",
      "An empty/loading Table, since the List is an empty list and Loading is True until the fetch finishes",
      "Records from the database server-rendered before the page appears",
    ],
    correctIndex: 2,
    explanation: "While fetching, the Aggregate's List is an empty list and its loading state is True, so widgets bound to it show an empty/loading state; no error occurs and stale prior-screen data is not shown.",
  },
  {
    id: 63,
    domain: "screen-lifecycle",
    question: "A developer wants to ensure logic in OnAfterFetch only acts on a successful fetch and handles the no-records case gracefully. What is true about OnAfterFetch firing?",
    options: [
      "OnAfterFetch fires after the fetch completes whether or not any records were returned",
      "OnAfterFetch fires only when one or more records are returned",
      "OnAfterFetch fires before the fetch, so it cannot know the record count",
      "OnAfterFetch fires only after Refresh Data, never on initial load",
    ],
    correctIndex: 0,
    explanation: "OnAfterFetch fires once the fetch completes regardless of whether records were returned, so the handler should check List length to handle the empty case; it runs on both initial load and refreshes.",
  },
  {
    id: 64,
    domain: "screen-lifecycle",
    question: "A developer puts an Update entity action inside a screen's OnInitialize flow. The action does not compile or is disallowed. Why?",
    options: [
      "Update actions require OnAfterFetch to be present on the screen first",
      "OnInitialize only allows Aggregates, not entity actions",
      "OnInitialize runs too early for any logic, so all nodes are disallowed",
      "Entity Create/Update/Delete are server-side and cannot be placed directly in the client-side OnInitialize flow; use a Server Action via Run Server Action",
    ],
    correctIndex: 3,
    explanation: "OnInitialize is client-side logic, and entity Create/Update/Delete run on the server, so they must be wrapped in a Server Action and invoked from the client flow. OnInitialize allows client logic, just not server-only nodes directly.",
  },
  {
    id: 65,
    domain: "screen-lifecycle",
    question: "A parent screen embeds a Block and binds the Block's input to a screen Local Variable. The screen later changes that Local Variable. Which sequence correctly describes what happens in the Block?",
    options: [
      "Nothing happens because Blocks only read their inputs once at creation",
      "The Block's OnRender fires but the input value stays at its original value",
      "The Block is destroyed and recreated, firing OnInitialize again",
      "The Block's OnParametersChanged fires, and any Aggregate referencing the input re-fetches",
    ],
    correctIndex: 3,
    explanation: "Changing the bound value updates the Block's input, firing OnParametersChanged (not OnInitialize, since the Block isn't recreated) and triggering a re-fetch of any Aggregate whose definition references that input.",
  },
  {
    id: 66,
    domain: "screen-lifecycle",
    question: "A developer needs cleanup logic (e.g., clearing a timer) to run when the user navigates away from a Reactive screen. Which event is appropriate?",
    options: [
      "OnDestroy",
      "OnAfterFetch",
      "OnRender",
      "OnInitialize",
    ],
    correctIndex: 0,
    explanation: "OnDestroy runs when the screen or block is being destroyed (e.g., navigating away), making it the correct place for teardown/cleanup logic. OnInitialize and OnRender relate to creation/rendering, not teardown.",
  },

  // ── Client vs Server Logic ──
  {
    id: 67,
    domain: "client-server-logic",
    question: "A developer wants the result of a server-side calculation to be available the instant a screen renders, before any user interaction. Which approach correctly fetches it on the server during screen load?",
    options: [
      "Place the Server Action directly in the screen's OnRender client event",
      "Add a Run JavaScript node to the screen's OnReady event",
      "Add a Data Action to the screen that calls the Server Action",
      "Declare the calculation as a Local Variable default value",
    ],
    correctIndex: 2,
    explanation: "A Data Action runs server-side as part of the screen's data fetch during screen load and is the supported way to compute/fetch data on render before user interaction. The other options are client events or cannot host server logic.",
  },
  {
    id: 68,
    domain: "client-server-logic",
    question: "Inside a Server Action flow, a developer drags an element to show 'Saved successfully' to the end user. Why is the Message node unavailable there?",
    options: [
      "Message must be wrapped in a Run Server Action node",
      "Message is a client-side UI feedback element and the screen is not in scope on the server",
      "Message requires an Aggregate to be present first",
      "Message can only be used inside Exception Handlers",
    ],
    correctIndex: 1,
    explanation: "The Message node provides UI feedback in the browser, so it is client-only and unavailable in a Server Action where there is no screen/browser context. Aggregate and Exception Handler options are unrelated distractors.",
  },
  {
    id: 69,
    domain: "client-server-logic",
    question: "A Client Action calls Server Action A, which internally calls Server Action B. How many server round-trips does this produce?",
    options: [
      "Three round-trips, one per action",
      "Zero, because nested calls run on the client",
      "One round-trip for the whole call to A",
      "Two round-trips, one for A and one for B",
    ],
    correctIndex: 2,
    explanation: "Crossing the client-to-server boundary once (Run Server Action to A) is a single round-trip; A calling B executes entirely on the server within that same request, adding no round-trips.",
  },
  {
    id: 70,
    domain: "client-server-logic",
    question: "A developer needs to return a list of records plus a computed total from one reusable piece of logic that queries the database. Which element should hold this logic?",
    options: [
      "A Client Action with an Aggregate",
      "A Static Entity action",
      "A Server Action with multiple output parameters",
      "A Run JavaScript node",
    ],
    correctIndex: 2,
    explanation: "A Server Action can contain an Aggregate for the query and expose multiple output parameters (a List plus an Integer total). Client Actions cannot contain Aggregates.",
  },
  {
    id: 71,
    domain: "client-server-logic",
    question: "Which node can appear in BOTH Client Action and Server Action flows in a Reactive Web App?",
    options: [
      "For Each",
      "SQL",
      "Aggregate",
      "Run JavaScript",
    ],
    correctIndex: 0,
    explanation: "For Each, like If, Switch, and Assign, is a flow-control element valid in both client and server logic. Aggregate and SQL are server-only and Run JavaScript is client-only.",
  },
  {
    id: 72,
    domain: "client-server-logic",
    question: "A developer assigns the output of a Run Server Action node to a screen Local Variable, then immediately reads that variable in the next Assign. Is the value available?",
    options: [
      "No, you must use an OnAfterFetch event to read it",
      "No, output values are only available after the screen reloads",
      "Yes, output parameters are populated when the Run Server Action completes",
      "Only if the Server Action contains a Commit",
    ],
    correctIndex: 2,
    explanation: "When a Run Server Action node completes, its output parameters are returned and immediately usable by subsequent nodes in the same client flow. OnAfterFetch applies to data fetches, not Run Server Action.",
  },
  {
    id: 73,
    domain: "client-server-logic",
    question: "A developer puts an Assign node in a Client Action to set Local.Counter = Local.Counter + 1. Where does this arithmetic execute?",
    options: [
      "In the browser, with no server involvement",
      "On the server only if Counter is an Integer",
      "On the server, because Assign always triggers a round-trip",
      "On the database, via an implicit SQL statement",
    ],
    correctIndex: 0,
    explanation: "Assign and basic arithmetic expressions in a Client Action execute in the browser with no round-trip; a round-trip only occurs when a Server Action is invoked.",
  },
  {
    id: 74,
    domain: "client-server-logic",
    question: "Which statement about calling a Client Action from within a Server Action is correct?",
    options: [
      "It is allowed only if the Client Action has no Message node",
      "It is not allowed; Server Actions cannot call Client Actions",
      "It is allowed and runs the Client Action on the server",
      "It is allowed but the Client Action runs in the browser",
    ],
    correctIndex: 1,
    explanation: "Server logic runs on the server with no browser context, so a Server Action cannot call a Client Action. The reverse (client calling server) is supported via Run Server Action.",
  },
  {
    id: 75,
    domain: "client-server-logic",
    question: "A developer wants a piece of logic to be callable from another module as a strongly decoupled, server-side service that is auto-detected for impact analysis. Which element fits best?",
    options: [
      "A Public Client Action",
      "A Service Action exposed by a Service module",
      "An Aggregate marked Public",
      "A Static Entity",
    ],
    correctIndex: 1,
    explanation: "A Service Action exposed by a Service module runs server-side, provides a strongly decoupled (weak-dependency) contract between modules, and is tracked for impact analysis. Client Actions run in the browser and cannot be a decoupled service contract.",
  },
  {
    id: 76,
    domain: "client-server-logic",
    question: "In a Client Action, a developer adds a Switch node branching on Local.OrderStatus. What is true about this Switch?",
    options: [
      "It evaluates its conditions in the browser at runtime",
      "It can only be used in Server Actions because branching needs the server",
      "It requires an Aggregate to supply the branch value",
      "It commits the transaction on the default branch",
    ],
    correctIndex: 0,
    explanation: "Switch is a shared flow-control element; inside a Client Action it evaluates its conditions in the browser at runtime. It needs no Aggregate and does not commit transactions.",
  },
  {
    id: 77,
    domain: "client-server-logic",
    question: "A developer drags a Server Action named 'CalculateTax' onto a Client Action flow. What does Service Studio show in the flow?",
    options: [
      "A Message node prompting for parameters",
      "An Aggregate placeholder",
      "A Run Server Action node representing the call",
      "The Server Action's internal flow inlined into the Client Action",
    ],
    correctIndex: 2,
    explanation: "Dropping a Server Action onto a client flow creates a Run Server Action node representing the call and the server round-trip; the server action's internal flow is never inlined.",
  },
  {
    id: 78,
    domain: "client-server-logic",
    question: "What is the correct reason an Aggregate cannot be added to a Client Action?",
    options: [
      "Aggregates only return Static Entities",
      "Aggregates are deprecated in Reactive Web",
      "Aggregates require a Message node to display results",
      "The browser has no direct database connection, so data access stays server-side",
    ],
    correctIndex: 3,
    explanation: "Data access (Aggregates and SQL) runs server-side because the browser has no direct database connection; client logic reaches data through Server Actions. Aggregates are not deprecated and are not limited to Static Entities.",
  },
  {
    id: 79,
    domain: "client-server-logic",
    question: "A developer needs to loop over a List returned by a Server Action and modify a Text Local Variable on the client. Which combination is valid in the Client Action?",
    options: [
      "For Each + Message inside the loop to mutate the variable",
      "Aggregate + For Each",
      "SQL + Assign",
      "For Each + Assign",
    ],
    correctIndex: 3,
    explanation: "For Each and Assign are both valid client-side elements, so iterating the returned list and updating a Text Local Variable works in a Client Action. Aggregate and SQL are server-only, and Message cannot mutate variables.",
  },
  {
    id: 80,
    domain: "client-server-logic",
    question: "Which data type CANNOT be used as an output parameter of a Server Action?",
    options: [
      "A Structure",
      "A Screen",
      "A Record",
      "A List of an Entity",
    ],
    correctIndex: 1,
    explanation: "Action parameters accept basic types, Entities, Structures, Records, and Lists of these, but a Screen is a UI element, not a data type, so it cannot be an output parameter.",
  },
  {
    id: 81,
    domain: "client-server-logic",
    question: "A developer wants client-side validation to run before a Server Action saves data, aborting the save when invalid. Which design keeps the round-trip from happening when input is invalid?",
    options: [
      "Put the validation If inside the Server Action after Run Server Action",
      "Use an If in the Client Action to skip the Run Server Action when validation fails",
      "Add an Aggregate before the If to validate",
      "Rely on the database to reject invalid rows",
    ],
    correctIndex: 1,
    explanation: "Using an If in the Client Action to skip the Run Server Action when validation fails avoids the round-trip entirely. Validating inside the Server Action means the round-trip already occurred.",
  },
  {
    id: 82,
    domain: "client-server-logic",
    question: "True or false: A Server Action can contain a Run JavaScript node so it can run client-side scripts on the server.",
    options: [
      "False, Run JavaScript is client-only and cannot be added to Server Actions",
      "False, but only because it needs a Message node",
      "True, but only for Static Entities",
      "True, JavaScript runs identically on both tiers",
    ],
    correctIndex: 0,
    explanation: "Run JavaScript executes in the browser and is client-only, so it cannot be added to a Server Action; JavaScript does not run on the server tier in Reactive Web.",
  },
  {
    id: 83,
    domain: "client-server-logic",
    question: "A Local Variable in a Server Action holds intermediate data during the flow. After the action returns its outputs, what happens to that Local Variable?",
    options: [
      "It is sent back to the client as a hidden parameter",
      "It ceases to exist when the action execution ends",
      "It is persisted to the database automatically",
      "It is cached in the session for the next call",
    ],
    correctIndex: 1,
    explanation: "Local Variables are scoped to a single execution of the action and are discarded when it ends; only declared output parameters are returned. They are never auto-persisted or session-cached.",
  },
  {
    id: 84,
    domain: "client-server-logic",
    question: "A developer calls a Server Action from a Client Action but forgets to assign its output to anything. What happens?",
    options: [
      "The round-trip is skipped to optimize performance",
      "The action runs on the client instead",
      "The server logic still executes; the returned outputs are simply discarded",
      "A compile error blocks publishing",
    ],
    correctIndex: 2,
    explanation: "The Server Action still executes on the server via the round-trip; ignoring its outputs is allowed and the returned values are simply discarded. OutSystems does not require capturing outputs.",
  },
  {
    id: 85,
    domain: "client-server-logic",
    question: "Which scenario correctly requires a Server Action rather than a Client Action?",
    options: [
      "Looping over an in-memory list to count items",
      "Concatenating two text inputs for display",
      "Deleting a record from an Entity",
      "Toggling a boolean Local Variable to show/hide a section",
    ],
    correctIndex: 2,
    explanation: "Deleting a record from an Entity is database access, which must occur in a Server Action. Toggling variables, concatenating text, and in-memory loops are client-side operations.",
  },
  {
    id: 86,
    domain: "client-server-logic",
    question: "A developer adds an If node in a Server Action. On which tier is its condition evaluated?",
    options: [
      "On the server, since the If is part of server logic",
      "Always in the browser for responsiveness",
      "On the database engine",
      "On both tiers simultaneously",
    ],
    correctIndex: 0,
    explanation: "If is a shared element, but where it runs depends on its containing action; inside a Server Action the condition is evaluated on the server. The same If in a Client Action would run in the browser.",
  },
  {
    id: 87,
    domain: "client-server-logic",
    question: "What is required for a Client Action to read data from the database in a Reactive Web App?",
    options: [
      "Enabling a Run JavaScript node with a SQL string",
      "Marking the Client Action as Public",
      "Adding an Aggregate and setting its Fetch property to On Demand",
      "Calling a Server Action (e.g., via Run Server Action) that performs the query",
    ],
    correctIndex: 3,
    explanation: "A Client Action reaches the database only indirectly, by invoking a Server Action containing the Aggregate or SQL. Aggregates cannot be placed in a Client Action and Run JavaScript cannot query the database.",
  },
  {
    id: 88,
    domain: "client-server-logic",
    question: "A developer wants to refresh part of the screen after a Server Action updates data. Which sequence is correct in the Client Action?",
    options: [
      "Run JavaScript, then Commit",
      "Run Server Action, then Refresh Data / assign updated outputs to the bound variables",
      "Aggregate, then Message",
      "SQL node, then Refresh Data",
    ],
    correctIndex: 1,
    explanation: "After the Run Server Action round-trip completes, the client flow can use Refresh Data on the bound source or assign updated outputs to bound variables so the UI reflects the change. Aggregates/SQL cannot exist in a Client Action and there is no client Commit element.",
  },
  {
    id: 89,
    domain: "client-server-logic",
    question: "Which statement about input parameters of a Server Action is accurate?",
    options: [
      "They must all be Entity identifiers",
      "They are stored in the session until overwritten",
      "They are read directly from the browser DOM at runtime",
      "They are passed from the client to the server when the action is invoked",
    ],
    correctIndex: 3,
    explanation: "When a Client Action invokes a Server Action, the input parameter values are serialized and sent to the server for that call. They support many data types and are not read from the DOM, session-stored, or limited to identifiers.",
  },
  {
    id: 90,
    domain: "client-server-logic",
    question: "A developer needs the SAME validation logic, with no database access, in three different screens' Submit handlers. What minimizes duplication while avoiding an unnecessary round-trip?",
    options: [
      "A reusable Client Action invoked from each Submit handler",
      "Three separate Aggregates",
      "A Server Action called via Run Server Action in each handler",
      "A Static Entity holding the rules",
    ],
    correctIndex: 0,
    explanation: "Reusable validation logic with no database access belongs in a Client Action invoked from each Submit handler; it runs locally with no round-trip and avoids duplication. A Server Action would force an unneeded server call.",
  },

  // ── UI & Widgets ──
  {
    id: 91,
    domain: "ui-widgets",
    question: "A developer sets an Input's Mandatory property to True but leaves the default ValidationMessage empty. What happens when the user submits the empty field?",
    options: [
      "OutSystems shows a default built-in 'The X is mandatory' style message and blocks submission",
      "A runtime error is thrown for the missing message",
      "Nothing is shown, but Form.Valid still becomes False",
      "The form submits because no custom message was provided",
    ],
    correctIndex: 0,
    explanation: "A Mandatory Input uses a platform-provided default mandatory message when no custom ValidationMessage is set, and submission via a Submit button is still blocked (Form.Valid becomes False).",
  },
  {
    id: 92,
    domain: "ui-widgets",
    question: "In a Reactive Web Form, a developer sets a Button's Method property. Which value triggers the Form's built-in validation before the OnClick action runs?",
    options: [
      "Ajax",
      "Async",
      "Navigate",
      "Submit",
    ],
    correctIndex: 3,
    explanation: "A Button (or Link) with Method = Submit triggers the Form's built-in input validation before running the OnClick action. Navigate skips validation; Ajax/Async are not valid Method options for triggering form validation here.",
  },
  {
    id: 93,
    domain: "ui-widgets",
    question: "A developer needs a List to show a loading skeleton while its data source is still fetching. Which approach uses built-in capability?",
    options: [
      "Set the List's Loading property to True permanently",
      "Set the screen's IsLoading global variable",
      "Wrap the List in an If whose Condition checks the aggregate's not-loading state, showing a placeholder otherwise",
      "Use the List's built-in Spinner property",
    ],
    correctIndex: 2,
    explanation: "You drive the UI from the aggregate's loading/fetch state (an If checking the not-loading condition) to show a placeholder versus the List. There is no built-in 'Loading' or 'Spinner' property on the List widget nor a built-in screen IsLoading global.",
  },
  {
    id: 94,
    domain: "ui-widgets",
    question: "Inside a List bound to an aggregate GetOrders, a developer adds an Expression to show each row's customer name. Which is the correct binding?",
    options: [
      "List.Item.CustomerName",
      "GetOrders.Row.CustomerName",
      "GetOrders.List.Current.CustomerName",
      "GetOrders.Current.CustomerName",
    ],
    correctIndex: 2,
    explanation: "Inside the List's repeated content you reference the current iterated record via GetOrders.List.Current.CustomerName. The other accessors are not valid OutSystems notations.",
  },
  {
    id: 95,
    domain: "ui-widgets",
    question: "A developer wants a Link to be shown only to administrators. Which Link property should be bound to the permission Boolean?",
    options: [
      "Enabled",
      "Rendered",
      "Active",
      "Visible",
    ],
    correctIndex: 3,
    explanation: "Binding the Link's Visible property to the permission Boolean controls whether it is rendered. Enabled would still render it (disabled), and 'Active'/'Rendered' are not Link properties.",
  },
  {
    id: 96,
    domain: "ui-widgets",
    question: "A developer places a For Each widget directly on a screen and sets its List Records to an aggregate output. Where is the iterated record referenced inside it?",
    options: [
      "Via the aggregate's .List.Current within the For Each content",
      "Via the For Each widget's CurrentRowNumber only",
      "Via a mandatory Iterator local variable you must declare",
      "Via the global SystemCurrentRecord",
    ],
    correctIndex: 0,
    explanation: "The UI For Each iterates the bound list and exposes the current item via the source's .List.Current notation inside its content. No iterator variable is declared and there is no SystemCurrentRecord global.",
  },
  {
    id: 97,
    domain: "ui-widgets",
    question: "A developer marks a Block Event as NOT Mandatory and a consuming screen does not handle it. What happens when the Block triggers that event at runtime?",
    options: [
      "A runtime exception is raised on the screen",
      "The platform compiles with an error blocking publish",
      "The Block's parent automatically refreshes its data",
      "Nothing happens on the parent; the event is simply ignored",
    ],
    correctIndex: 3,
    explanation: "A non-mandatory Block Event with no handler simply does nothing when triggered; no exception, compile error, or automatic refresh occurs. (A Mandatory event without a handler would cause a publish-time error.)",
  },
  {
    id: 98,
    domain: "ui-widgets",
    question: "A developer wants to pass data INTO a Block from the parent screen. Which Block element is used?",
    options: [
      "A Block Input Parameter",
      "A Block Output Parameter",
      "A Block Event",
      "A Block Local Variable",
    ],
    correctIndex: 0,
    explanation: "Input Parameters carry data from the parent screen into the Block. Events notify the parent (child-to-parent), and Blocks do not expose Output Parameters for data.",
  },
  {
    id: 99,
    domain: "ui-widgets",
    question: "A developer uses an If widget in the UI with Condition = GetUsers.List.Empty to show an empty message. When the aggregate returns 3 records, which branch renders?",
    options: [
      "Both branches render but the empty one is hidden",
      "Neither branch renders",
      "The False branch (the list content)",
      "The True branch (empty message)",
    ],
    correctIndex: 2,
    explanation: "With 3 records, GetUsers.List.Empty is False, so the If renders its False branch (the list content). The True branch is not rendered into the DOM.",
  },
  {
    id: 100,
    domain: "ui-widgets",
    question: "A developer adds two Buttons to a Form: 'Save' and 'Cancel'. Cancel should navigate away WITHOUT running validation. How should Cancel's Method be set?",
    options: [
      "Method = Submit but set Form.Valid = True first",
      "Method = Ajax with Validate = False",
      "Method = Navigate",
      "Method = Submit so validation is skipped",
    ],
    correctIndex: 2,
    explanation: "Method = Navigate performs navigation without triggering the Form's input validation, which is what a Cancel button needs. Submit would run validation.",
  },
  {
    id: 101,
    domain: "ui-widgets",
    question: "A developer wants a 'Delete' Link inside each ListItem to call DeleteOrder with that row's Id. Inside the loop, what should be passed as the action's input?",
    options: [
      "A hardcoded literal Id",
      "The screen's SelectedOrderId session variable",
      "The For Each global index number",
      "GetOrders.List.Current.Order.Id",
    ],
    correctIndex: 3,
    explanation: "Inside the loop you pass the current record's identifier (GetOrders.List.Current.Order.Id) so each row deletes its own record. A session variable, literal, or loop index would not correctly target the per-row record.",
  },
  {
    id: 102,
    domain: "ui-widgets",
    question: "A developer wants validation to fail for an Input when a custom business rule is violated (not just empty). What is the correct pattern in the action that runs on submit?",
    options: [
      "Set the Input widget's Valid = False and its ValidationMessage, then check Form.Valid",
      "Set Form.HasErrors = True",
      "Throw a UserException from the client action",
      "Set the Input's Mandatory = True at runtime",
    ],
    correctIndex: 0,
    explanation: "For custom validation you set the specific Input's Valid = False and assign its ValidationMessage so the message appears under it; Form.Valid then reflects the failure. Exceptions, runtime Mandatory toggling, and 'Form.HasErrors' are not the mechanism.",
  },
  {
    id: 103,
    domain: "ui-widgets",
    question: "What is the difference between an Input widget's Prompt property and its Variable property?",
    options: [
      "Prompt is mandatory; Variable is optional",
      "Prompt is the placeholder/hint text; Variable is the two-way-bound stored value",
      "Both store the value; one is client-side and one server-side",
      "Prompt holds the value; Variable holds placeholder text",
    ],
    correctIndex: 1,
    explanation: "Prompt is the placeholder/hint text shown when the field is empty; Variable is the two-way binding to the stored value. Neither is inherently mandatory vs optional.",
  },
  {
    id: 104,
    domain: "ui-widgets",
    question: "A developer notices that an If widget's True branch keeps its child widgets' state lost when the condition toggles back and forth. Why?",
    options: [
      "This indicates a platform bug requiring a Refresh",
      "The If only hides branches with CSS, so state is preserved",
      "If widgets cache widget state in a session variable",
      "The non-matching branch is removed from the DOM, so its widgets are re-created when shown again",
    ],
    correctIndex: 3,
    explanation: "The If widget renders only the matching branch into the DOM and removes the other; toggling re-creates the branch's widgets, so transient state is not preserved. It is not CSS-hidden nor cached in a session variable.",
  },
  {
    id: 105,
    domain: "ui-widgets",
    question: "A developer wants a List to refresh after a record is added via a client action. What must be done to reflect the new data?",
    options: [
      "Set the List's AutoRefresh property to True",
      "Re-drag the List widget",
      "Reload the entire application module",
      "Call Refresh Data on the aggregate that feeds the List",
    ],
    correctIndex: 3,
    explanation: "Calling the Refresh Data node on the source aggregate re-fetches its records, updating the bound List. There is no 'AutoRefresh' List property; re-dragging or reloading the module is unnecessary.",
  },
  {
    id: 106,
    domain: "ui-widgets",
    question: "A developer wants a Button labeled with a dynamic count, e.g. 'Items (5)'. How is the Button caption made dynamic in Reactive Web?",
    options: [
      "Place an Expression widget inside the Button to render the dynamic text",
      "Set the Button's Text property to a JavaScript template",
      "Bind a session variable to the Button's Title property",
      "Buttons only accept static text in the Label property",
    ],
    correctIndex: 0,
    explanation: "You place an Expression widget inside the Button so its caption renders a dynamic value. Buttons are not limited to static text, and there is no Text/Title JavaScript-template approach.",
  },
  {
    id: 107,
    domain: "ui-widgets",
    question: "A developer adds a Checkbox bound to a Boolean Local Variable and wants an action to run whenever the user toggles it. Which event is used?",
    options: [
      "OnClick of the surrounding Container",
      "OnChange of the Checkbox",
      "OnSubmit of the parent Form",
      "OnToggle of the Checkbox",
    ],
    correctIndex: 1,
    explanation: "The Checkbox's OnChange event fires when its checked state changes, the correct hook to run a client action. There is no OnToggle event.",
  },
  {
    id: 108,
    domain: "ui-widgets",
    question: "A developer wants a screen's URL to receive a parameter when a Link navigates to a detail screen. How is the value supplied?",
    options: [
      "By concatenating it into the Link's Style Classes",
      "Detail screens cannot receive parameters from Links",
      "By storing it in a global the detail screen reads automatically",
      "By setting the destination screen's Input Parameter on the Link's navigation node",
    ],
    correctIndex: 3,
    explanation: "When a Link navigates to a screen with Input Parameters, you supply the values on the navigation/Destination node and they become part of the route/URL. Globals, Style Classes, and 'not possible' are incorrect.",
  },

  // ── Exceptions & Debug ──
  {
    id: 109,
    domain: "exception-debug",
    question: "A developer raises a custom User Exception named 'InvalidDiscount' in a Server Action. In the calling Screen Action flow, the developer wants to catch ONLY that specific exception and ignore all others. Which Exception Handler should be added?",
    options: [
      "An AllExceptions handler with a condition checking the exception name",
      "A Raise Exception node configured to catch InvalidDiscount",
      "A handler whose 'Exception' property is set to the InvalidDiscount User Exception",
      "A DatabaseException handler, since User Exceptions inherit from it",
    ],
    correctIndex: 2,
    explanation: "An Exception Handler catches a specific exception when its 'Exception' property is set to that exception type (here the InvalidDiscount User Exception). A Raise Exception node throws rather than catches, and AllExceptions catches everything.",
  },
  {
    id: 110,
    domain: "exception-debug",
    question: "Inside an Exception Handler flow, a developer wants to display the actual error text that was raised. Which built-in variable holds that message?",
    options: [
      "System.ErrorText",
      "LastError.Description",
      "ExceptionMessage",
      "Exception.Message",
    ],
    correctIndex: 2,
    explanation: "Within an Exception Handler's scope, the built-in 'ExceptionMessage' variable holds the message of the caught exception. The other names are not real OutSystems built-in variables.",
  },
  {
    id: 111,
    domain: "exception-debug",
    question: "A developer places an Exception Handler with its Exception set to 'AllExceptions' first in the flow, and a more specific 'SecurityException' handler second. A SecurityException is raised. Which handler catches it?",
    options: [
      "Neither, because two handlers in one flow cause a compile error",
      "Both handlers run in sequence",
      "The AllExceptions handler, because it was placed first in the flow",
      "The SecurityException handler, because OutSystems uses the most specific matching handler regardless of order",
    ],
    correctIndex: 3,
    explanation: "OutSystems selects the most specific matching Exception Handler regardless of the visual order in which handlers were placed, so the SecurityException handler catches it.",
  },
  {
    id: 112,
    domain: "exception-debug",
    question: "What does the 'Abort Transaction' property of an Exception Handler control?",
    options: [
      "Whether the error is written to Service Center",
      "Whether the database transaction up to that point is rolled back when the exception is handled",
      "Whether the exception is re-raised to the parent flow automatically",
      "Whether the user is shown a confirmation dialog before the error",
    ],
    correctIndex: 1,
    explanation: "The Exception Handler's 'Abort Transaction' property controls whether the implicit database transaction up to that point is rolled back. Logging is controlled by 'Log Error'.",
  },
  {
    id: 113,
    domain: "exception-debug",
    question: "A Server Action performs a CreateOrder followed by a CreateOrderItem, and an exception occurs during CreateOrderItem. By default (Abort Transaction = Yes in the handler), what happens to the already-executed CreateOrder?",
    options: [
      "It is rolled back together with the rest of the transaction",
      "It is retried automatically after the handler completes",
      "It is committed independently and remains in the database",
      "It is converted into a warning record",
    ],
    correctIndex: 0,
    explanation: "All database writes in a single server request run in one implicit transaction, so when the handler aborts the transaction the earlier CreateOrder is rolled back along with everything else.",
  },
  {
    id: 114,
    domain: "exception-debug",
    question: "A developer calls a REST API consumed action and wants to catch failures caused by the remote service being unreachable. Which built-in exception type should the Exception Handler target?",
    options: [
      "InvalidInputException",
      "CommunicationException",
      "UserException",
      "DatabaseException",
    ],
    correctIndex: 1,
    explanation: "CommunicationException is raised for integration/connectivity failures such as an unreachable REST/SOAP endpoint. DatabaseException is for data-source errors and UserException for deliberately raised business errors.",
  },
  {
    id: 115,
    domain: "exception-debug",
    question: "While debugging a published module, a developer sets a breakpoint and triggers the flow from the browser, but execution never pauses. Which is the most likely cause?",
    options: [
      "Breakpoints only work in modules opened locally, never on a published environment",
      "Breakpoints require Log Error to be set to Yes",
      "The developer is not connected to the same environment where the breakpoint was set, or the version running differs from the one open in Service Studio",
      "Client-side flows cannot be debugged at all",
    ],
    correctIndex: 2,
    explanation: "The debugger only pauses when the running version matches the one open in Service Studio on the connected environment; a different environment or a stale version causes breakpoints to be skipped. OutSystems does support debugging published modules and client-side logic.",
  },
  {
    id: 116,
    domain: "exception-debug",
    question: "In the OutSystems debugger, a developer has stepped into a called Server Action and now wants to finish that action and return to the caller in one step. Which command does this?",
    options: [
      "Step Over",
      "Continue",
      "Step Into",
      "Step Out",
    ],
    correctIndex: 3,
    explanation: "Step Out runs the rest of the current action and returns control to the calling flow in one step. Step Over executes a single node without entering calls, and Continue runs to the next breakpoint.",
  },
  {
    id: 117,
    domain: "exception-debug",
    question: "A developer wants the debugger to pause only when a loop variable reaches a certain value, rather than on every iteration. Which debugger feature supports this?",
    options: [
      "The Step Over command",
      "A conditional breakpoint with a boolean expression",
      "A Trace node inside the loop",
      "Setting Log Error to Yes on the loop",
    ],
    correctIndex: 1,
    explanation: "OutSystems supports conditional breakpoints, which pause execution only when a specified boolean expression evaluates to True. Log Error and Trace nodes do not control the debugger.",
  },
  {
    id: 118,
    domain: "exception-debug",
    question: "A developer raises a User Exception in a Screen Action but provides no Exception Handler anywhere in that flow or its callers. What does the end user experience?",
    options: [
      "The flow silently continues to the next node",
      "A validation message appears next to the relevant input field",
      "A generic error screen/message is shown and the error is logged in Service Center",
      "The browser tab is automatically closed",
    ],
    correctIndex: 2,
    explanation: "An uncaught exception propagates to the top, where the platform shows a generic error screen/message and logs it in Service Center. It is not silently ignored and does not map to a field-level validation message.",
  },
  {
    id: 119,
    domain: "exception-debug",
    question: "Which statement correctly distinguishes the 'Log Error' Exception Handler property from a 'Log Message' node?",
    options: [
      "They are identical and interchangeable",
      "Log Error suppresses the exception; Log Message re-raises it",
      "Log Error writes to the browser console only; Log Message writes to the database",
      "Log Error records an error-level entry tied to the exception; a Log Message node writes a general-purpose entry of a chosen type to the platform log",
    ],
    correctIndex: 3,
    explanation: "Setting Log Error = Yes on a handler records the caught exception as an error-level entry, while a Log Message node writes a general-purpose entry of a chosen type. Neither suppresses or re-raises the exception by itself.",
  },
  {
    id: 120,
    domain: "exception-debug",
    question: "A developer needs an Exception Handler that catches any database-related error but lets all other exceptions propagate untouched. Which single handler accomplishes this?",
    options: [
      "A UserException handler",
      "An AllExceptions handler",
      "A handler with Exception set to AllExceptions plus Abort Transaction = No",
      "A handler with Exception set to DatabaseException",
    ],
    correctIndex: 3,
    explanation: "A handler with Exception set to DatabaseException catches only database errors and lets all other exceptions propagate. AllExceptions would also catch unrelated exceptions, violating the requirement.",
  },
  {
    id: 121,
    domain: "exception-debug",
    question: "In Service Center's Monitoring > Errors detail view, which piece of information is typically available to help diagnose a logged exception?",
    options: [
      "A live editable copy of the running flow",
      "The end user's plaintext password",
      "The full source code of the Screen",
      "The error message plus context such as module, the eSpace/action where it occurred, and timestamp",
    ],
    correctIndex: 3,
    explanation: "A Service Center error log entry shows diagnostic context such as the message, the module/action where it occurred, environment, and timestamp. It never exposes passwords or editable source/flow access.",
  },
  {
    id: 122,
    domain: "exception-debug",
    question: "A developer adds an Exception Handler to a Screen's OnInitialize action and another to the same Screen's separate 'SaveButton' Screen Action. An exception raised inside SaveButton's flow is caught by which handler?",
    options: [
      "The OnInitialize handler, because it runs first",
      "The handler within the SaveButton Screen Action's own flow",
      "Both handlers, in sequence",
      "Neither, because Screen Actions cannot contain Exception Handlers",
    ],
    correctIndex: 1,
    explanation: "Exception Handlers only catch exceptions within the scope of the flow that contains them, so SaveButton's exception is caught by SaveButton's own handler, not the unrelated OnInitialize handler.",
  },
  {
    id: 123,
    domain: "exception-debug",
    question: "After catching an exception in an AllExceptions handler and showing the user a friendly message, the developer does NOT add a Raise Exception node and ends the handler with a normal End. What is the effect on the transaction by default?",
    options: [
      "The exception is re-thrown to the caller automatically",
      "The handler loops back to the start of the flow",
      "The application restarts the user session",
      "The exception is considered handled and the flow completes normally without propagating further",
    ],
    correctIndex: 3,
    explanation: "Ending an Exception Handler with a normal End marks the exception as handled, so the flow completes without propagating further. Re-raising to the caller requires an explicit Raise Exception node.",
  },
  {
    id: 124,
    domain: "exception-debug",
    question: "A developer wants to deliberately raise an existing built-in exception 'SecurityException' from within an action to block unauthorized access. Which approach is valid?",
    options: [
      "Call the system action ThrowSecurityException",
      "Set a Local Variable named SecurityException to True",
      "Use a Raise Exception node and select the SecurityException type",
      "Add a SecurityException Exception Handler, which raises it",
    ],
    correctIndex: 2,
    explanation: "A Raise Exception node lets the developer throw a chosen exception type, including built-in ones like SecurityException. An Exception Handler catches rather than raises, and there is no ThrowSecurityException system action.",
  },
  {
    id: 125,
    domain: "exception-debug",
    question: "During debugging, a developer wants to change the value of a Local Variable mid-flow to test a different code path without restarting. In the OutSystems debugger, this is done by:",
    options: [
      "Editing the variable's value in the debugger's variables/inspection pane while execution is paused",
      "It is impossible; variables are read-only during debugging",
      "Recompiling the module from the Service Center console",
      "Setting Log Error to Yes on the node",
    ],
    correctIndex: 0,
    explanation: "While paused at a breakpoint, the OutSystems debugger lets the developer inspect and edit variable values in the inspection pane to test alternate paths. Variables are not read-only during debugging.",
  },
  {
    id: 126,
    domain: "exception-debug",
    question: "An exception is raised inside a Timer's action flow that has no Exception Handler. How is this surfaced, given a Timer has no interactive user?",
    options: [
      "A pop-up appears on the developer's screen immediately",
      "The Timer execution fails and the error is recorded in Service Center's error/timer logs",
      "The Timer silently succeeds and ignores the error",
      "The exception is forwarded to every logged-in user",
    ],
    correctIndex: 1,
    explanation: "Because a Timer runs server-side with no UI, an uncaught exception causes the timer run to fail and the error is logged in Service Center (visible in error and timer logs). There is no interactive pop-up.",
  },

  // ── Variables & Storage ──
  {
    id: 127,
    domain: "variables-storage",
    question: "A developer wants a user preference (e.g., a selected theme) to survive even after the user closes the browser and reopens the app on the SAME device, without writing to the database. Which storage element is purpose-built for this?",
    options: [
      "Site Property",
      "Local Variable",
      "Session Variable",
      "Client Variable",
    ],
    correctIndex: 3,
    explanation: "Client Variables are persisted on the device (browser local storage) and survive across sessions on that device without writing to the database. A Session Variable lives on the server and is lost when the session ends.",
  },
  {
    id: 128,
    domain: "variables-storage",
    question: "Where is the value of a Client Variable physically stored in a Reactive Web application?",
    options: [
      "In the application's database in a system table",
      "On the device, in the browser's local storage",
      "In a Static Entity bundled with the module",
      "On the server, tied to the user's session",
    ],
    correctIndex: 1,
    explanation: "Client Variables are stored on the client device in the browser's local storage, which is why they are per-device and persist between sessions. Session Variables, by contrast, are kept on the server.",
  },
  {
    id: 129,
    domain: "variables-storage",
    question: "A developer adds a numeric Input Parameter to a Screen and marks its Is Mandatory property as No. What is the practical effect when navigating to that Screen without supplying the parameter?",
    options: [
      "Service Studio refuses to compile the navigation link",
      "The Screen uses the parameter's default value instead",
      "The parameter is automatically promoted to a Session Variable",
      "The Screen throws a runtime null reference error on initialization",
    ],
    correctIndex: 1,
    explanation: "A non-mandatory Input Parameter is optional on navigation and falls back to its configured default value when not supplied. Mandatory parameters must be set on the link.",
  },
  {
    id: 130,
    domain: "variables-storage",
    question: "A developer needs to know whether an Aggregate set to 'Only on Demand' has already completed its fetch before reading its results. Which built-in Aggregate property exposes this?",
    options: [
      "IsDataFetched",
      "Length",
      "Empty",
      "Count",
    ],
    correctIndex: 0,
    explanation: "An Aggregate/Data Action exposes the IsDataFetched boolean, which is True once the fetch has completed. Count returns record count but does not indicate whether the fetch has run.",
  },
  {
    id: 131,
    domain: "variables-storage",
    question: "In a Reactive Web Screen, a developer wants the total number of rows the database matched for an Aggregate, ignoring the Max Records limit applied to what was returned. Which Aggregate output should be used?",
    options: [
      "Get<Aggregate>.IsDataFetched",
      "Get<Aggregate>.List.Current",
      "Get<Aggregate>.Count",
      "Get<Aggregate>.List.Length",
    ],
    correctIndex: 2,
    explanation: "The Aggregate's Count returns the total number of records matching the query on the server, independent of Max Records. List.Length only counts the rows actually returned to the client, which can be capped by Max Records.",
  },
  {
    id: 132,
    domain: "variables-storage",
    question: "A developer sets an Aggregate's Max Records property to 50. What is the effect on the data the Screen receives?",
    options: [
      "At most 50 records are returned to and cached on the client",
      "The Aggregate raises an error if more than 50 records exist",
      "The database stores only 50 records total in the entity",
      "The Aggregate refreshes itself every 50 seconds",
    ],
    correctIndex: 0,
    explanation: "Max Records caps how many records the Aggregate returns to and caches on the client, improving performance for large result sets. It does not change underlying data or trigger errors.",
  },
  {
    id: 133,
    domain: "variables-storage",
    question: "A Local Variable of Boolean type is added to a Screen with no default value configured. What value does it hold when the Screen first initializes?",
    options: [
      "The previous value from the last time the Screen was open",
      "False",
      "True",
      "Null, which throws an error when read",
    ],
    correctIndex: 1,
    explanation: "OutSystems variables initialize to their type's default; for Boolean that default is False. Booleans are never null, and Local Variables are re-initialized each time the screen opens.",
  },
  {
    id: 134,
    domain: "variables-storage",
    question: "A developer wants to store a frequently accessed, fairly large data structure for one user across several screens but is concerned about performance. Why are Session Variables a poor choice for large objects?",
    options: [
      "Session Variable data is loaded into memory for the user's session and large values increase server memory usage",
      "Session Variables can only hold Text data type",
      "Session Variables are read-only after the first assignment",
      "Session Variables are shared across all users, causing collisions",
    ],
    correctIndex: 0,
    explanation: "Session Variables are kept in server-side session memory, so storing large structures increases per-user memory consumption and can hurt scalability. They are not read-only and not shared across users.",
  },
  {
    id: 135,
    domain: "variables-storage",
    question: "A developer needs to pass several values from a Client Action back to the Screen that called it. Which mechanism is correct?",
    options: [
      "Store the values in the action's own Local Variables",
      "Define Output Parameters on the Client Action",
      "Assign the values to the Screen's Input Parameters from inside the action",
      "Mark the action's Local Variables as Public",
    ],
    correctIndex: 1,
    explanation: "Output Parameters are the defined contract for returning values from an action to its caller. Local Variables are private to the action, and screen Input Parameters are set on navigation, not written back from a child action.",
  },
  {
    id: 136,
    domain: "variables-storage",
    question: "A developer changes a Site Property's value in Service Center while the app is running. When do users see the new value?",
    options: [
      "Immediately, since Site Properties are read at runtime",
      "Never; Site Property values are fixed at compile time",
      "Only for users who log out and log back in",
      "Only after the module is republished from Service Studio",
    ],
    correctIndex: 0,
    explanation: "Site Properties are runtime-configurable in Service Center and the new value takes effect without republishing (subject to caching, generally near-immediately). This runtime editability distinguishes them from compile-time constants.",
  },
  {
    id: 137,
    domain: "variables-storage",
    question: "In Reactive Web, a developer wants to react to a Client Variable's change automatically wherever it is displayed. What behavior should they expect from data bindings to a Client Variable?",
    options: [
      "Bindings must be manually refreshed with Refresh Data",
      "UI bound to the Client Variable updates reactively when its value changes",
      "Changing the Client Variable does not affect already-rendered widgets",
      "Client Variables cannot be bound to widgets at all",
    ],
    correctIndex: 1,
    explanation: "Client-side variables, including Client Variables, participate in the reactive data binding model, so widgets bound to them update automatically when the value changes. Refresh Data applies to re-running Aggregates/Data Actions, not variable bindings.",
  },
  {
    id: 138,
    domain: "variables-storage",
    question: "A developer assigns a value to a Client Variable inside a Server Action. What is the problem with this approach?",
    options: [
      "Server Actions run on the server and cannot access client-side Client Variables",
      "Client Variables are read-only and cannot be assigned anywhere",
      "Client Variables can only hold Identifier data types",
      "It will overwrite the Client Variable for every user globally",
    ],
    correctIndex: 0,
    explanation: "Client Variables live on the device and are only accessible from client-side logic; Server Actions execute on the server and have no access to them. They must be set from Client Actions or screen logic.",
  },
  {
    id: 139,
    domain: "variables-storage",
    question: "After binding a List widget to an Aggregate, a developer adds a record to the underlying entity using a CreateRecord action, then stays on the same Screen. Why does the list NOT show the new record automatically?",
    options: [
      "The Aggregate's client-cached results are not re-queried until Refresh Data runs",
      "The List widget caches only the first row",
      "Aggregates ignore newly created records by design",
      "CreateRecord rolls back automatically on Reactive Web",
    ],
    correctIndex: 0,
    explanation: "The Aggregate's results are cached on the client from its last fetch; a server-side insert does not update that cache until Refresh Data re-runs the query. The create itself is committed, not rolled back.",
  },
  {
    id: 140,
    domain: "variables-storage",
    question: "A developer wants the same configuration value (an API base URL) to be the same for every user and rarely changed. Comparing Site Property and Client Variable, which statement is correct?",
    options: [
      "A Client Variable is server-stored and a Site Property is device-stored",
      "A Client Variable is better because it is shared across all users",
      "A Site Property is correct; it is module-wide and shared by all users, while a Client Variable is per-device and per-user",
      "Both behave identically, so either choice is equivalent",
    ],
    correctIndex: 2,
    explanation: "A Site Property is a single module-wide value shared by all users and editable in Service Center, which fits shared configuration. A Client Variable is stored per device and is per-user, so it cannot represent one global value.",
  },

  // ── App Lifecycle ──
  {
    id: 141,
    domain: "app-lifecycle",
    question: "A developer presses 1-Click Publish and the bottom panel shows the TrueChange tab counting through several stages before the module goes live. What is the primary purpose of the TrueChange tab during this process?",
    options: [
      "To list and validate errors and warnings detected across the module before and during compilation",
      "To configure which public elements are exposed to consumer modules",
      "To deploy the application to the Production environment via LifeTime",
      "To display the runtime error logs collected from end users",
    ],
    correctIndex: 0,
    explanation: "The TrueChange tab continuously validates the module and lists errors and warnings during compilation; errors must be fixed before publishing completes. Runtime end-user error logs live in Service Center, and LifeTime handles cross-environment deployment.",
  },
  {
    id: 142,
    domain: "app-lifecycle",
    question: "A developer in Service Studio wants to revert a module to a version that was published earlier today after a bad change. Where can they download or roll back to a previously published version of that module?",
    options: [
      "In the Manage Dependencies dialog of the consumer module",
      "In the TrueChange tab by clicking Undo",
      "In Service Center, under the module's Versions list for that environment",
      "In LifeTime, by editing the application's data model",
    ],
    correctIndex: 2,
    explanation: "Each environment's Service Center keeps a per-module Versions history from which a developer can download or republish (roll back to) a prior published version. Manage Dependencies only configures consumed elements; TrueChange's undo is a design-time editor action, not a published-version rollback.",
  },
  {
    id: 143,
    domain: "app-lifecycle",
    question: "Two developers are editing the same module connected to the same Development environment. Developer A publishes first; then Developer B publishes a version based on the older code. What does OutSystems do?",
    options: [
      "It creates two separate modules automatically",
      "It silently overwrites A's version with B's, discarding A's changes",
      "It blocks B permanently until A deletes their version",
      "It warns B about the conflict and offers to merge the two versions before publishing",
    ],
    correctIndex: 3,
    explanation: "When a newer published version exists, Service Studio detects the conflict on publish and prompts the developer to merge changes before publishing, preventing accidental loss of work. It does not silently overwrite or permanently block.",
  },
  {
    id: 144,
    domain: "app-lifecycle",
    question: "A developer wants to add a ready-made component such as a charts library or a Google Maps connector to their application instead of building it. Which OutSystems resource is intended for discovering and installing such reusable components?",
    options: [
      "Integration Studio",
      "Service Center",
      "The TrueChange tab",
      "The OutSystems Forge",
    ],
    correctIndex: 3,
    explanation: "The OutSystems Forge is the community repository of reusable components and connectors that are installed into an environment and then consumed via dependencies. Service Center administers an environment and Integration Studio builds extensions, but neither is the component marketplace.",
  },
  {
    id: 145,
    domain: "app-lifecycle",
    question: "A producer module marks a Server Action as Public, and a consumer adds it via Manage Dependencies. Later the producer developer changes the action from Public back to private. What happens to the consumer?",
    options: [
      "The action is automatically re-exposed to avoid breaking consumers",
      "Nothing changes until the consumer is deleted and recreated",
      "The consumer keeps working because the reference is cached forever",
      "The consumer module will report a broken reference / error once it refreshes or republishes",
    ],
    correctIndex: 3,
    explanation: "Removing the Public flag breaks consumers that depend on it; the consumer reports a broken reference in TrueChange once it refreshes or republishes. The platform does not auto-re-expose elements to protect consumers.",
  },
  {
    id: 146,
    domain: "app-lifecycle",
    question: "A developer opens Service Studio and sees options to create different module types. Which module type is intended primarily to hold reusable Entities and Server Actions with no end-user screens?",
    options: [
      "A Mobile App module",
      "A Blank screen template",
      "A Service module (Service eSpace)",
      "A Reactive Web App (UI) module",
    ],
    correctIndex: 2,
    explanation: "A Service module (Service eSpace) is designed for back-end reusable logic and data (entities, server actions, integrations) without UI, supporting layered architecture. Reactive Web App and Mobile modules are for building end-user interfaces.",
  },
  {
    id: 147,
    domain: "app-lifecycle",
    question: "During 1-Click Publish, the TrueChange tab reports only warnings (no errors). What is the result?",
    options: [
      "Only the screens with warnings are excluded from publishing",
      "Publishing is blocked until the warnings are fixed",
      "The module publishes successfully; warnings are advisory and do not stop publishing",
      "The module publishes but is automatically deactivated",
    ],
    correctIndex: 2,
    explanation: "Warnings are advisory and do not block 1-Click Publish; only errors stop publishing. The module goes live with warnings still listed for the developer to address later.",
  },
  {
    id: 148,
    domain: "app-lifecycle",
    question: "A team has Dev, Test, and Prod environments managed by LifeTime. A developer needs to move an application that is currently in Dev all the way to Prod. According to the standard staged pipeline, what is the correct path?",
    options: [
      "Copy the database from Dev to Prod manually",
      "Deploy directly from Dev to Prod, skipping Test",
      "1-Click Publish the module from Service Studio straight into Prod",
      "Deploy Dev to Test, validate, then Test to Prod through LifeTime",
    ],
    correctIndex: 3,
    explanation: "LifeTime promotes applications sequentially through the pipeline: Dev to Test (for validation), then Test to Prod. You cannot 1-Click Publish into Prod, and skipping Test bypasses the staged-deployment model.",
  },
  {
    id: 149,
    domain: "app-lifecycle",
    question: "A developer adds a dependency on a public Static Entity from another module to use its records in a dropdown. What access does adding the dependency give the consumer?",
    options: [
      "Nothing, because Static Entities cannot be shared between modules",
      "Permission to change the producer's bootstrapped values",
      "Read access to the Static Entity's records and its identifiers",
      "Full permission to add or delete records in the Static Entity at runtime",
    ],
    correctIndex: 2,
    explanation: "A consumer that references a public Static Entity can read its records and use its identifiers (e.g., in dropdowns and comparisons), but Static Entity records are defined at design time in the producer, not created or deleted at runtime.",
  },
  {
    id: 150,
    domain: "app-lifecycle",
    question: "A developer wants to know on which front-end server an application is running and to restart a stuck Timer for one environment. Which tool exposes this kind of operational, environment-specific information?",
    options: [
      "Service Center",
      "Manage Dependencies",
      "Service Studio",
      "The Forge",
    ],
    correctIndex: 0,
    explanation: "Service Center is the per-environment administration console showing front-end servers, timers, processes, integrations, and logs, and lets an admin manage/restart timers. Service Studio is the design-time IDE and has no runtime server view.",
  },
  {
    id: 151,
    domain: "app-lifecycle",
    question: "A consumer module references a public Entity from a producer. The producer developer adds a new attribute to that Entity and republishes. What must the consumer do to use the new attribute?",
    options: [
      "Nothing; new attributes appear automatically in the consumer's aggregates",
      "Delete the reference and re-add the entire producer module",
      "Refresh the dependency in Manage Dependencies and republish the consumer",
      "Convert the entity to a Static Entity first",
    ],
    correctIndex: 2,
    explanation: "The consumer holds a cached design-time reference, so a new producer attribute only becomes visible after the dependency is refreshed in Manage Dependencies and the consumer is republished. Changes do not propagate automatically.",
  },
  {
    id: 152,
    domain: "app-lifecycle",
    question: "A developer notices that publishing a module also caused a second module to be republished automatically. Which OutSystems concept explains why a dependent module may be recompiled when its producer changes?",
    options: [
      "The Forge installation queue",
      "The TrueChange undo history",
      "The application's multi-tenancy setting",
      "Strong (compile-time) dependencies between modules",
    ],
    correctIndex: 3,
    explanation: "A strong (compile-time) dependency requires the consumer to be recompiled when the producer's referenced definition changes, so the platform may republish dependents to keep them consistent. Multi-tenancy, undo history, and Forge are unrelated to recompilation chains.",
  },
  {
    id: 153,
    domain: "app-lifecycle",
    question: "A developer accidentally deletes a public Server Action in a producer module and republishes. Several consumer modules now fail to publish. What is the recommended first step to identify exactly what broke?",
    options: [
      "Open the consumer's TrueChange tab to see the listed broken references and their locations",
      "Restart the Production environment",
      "Re-add the consumers to a new application",
      "Refresh dependencies on the producer module",
    ],
    correctIndex: 0,
    explanation: "The consumer's TrueChange tab pinpoints each broken reference and where it is used, guiding the fix (restore the element or update the consumer). Restarting Prod and re-grouping applications do not surface the cause.",
  },
  {
    id: 154,
    domain: "app-lifecycle",
    question: "When a developer creates a new application in Service Studio, OutSystems automatically creates at least one of which artifact inside it so development can begin?",
    options: [
      "A LifeTime deployment plan",
      "A module (eSpace)",
      "A Forge component",
      "A Service Center environment",
    ],
    correctIndex: 1,
    explanation: "An application is a container that must hold at least one module (eSpace); the module is what developers actually edit and publish. LifeTime plans, environments, and Forge components are not created automatically inside a new application.",
  },
  {
    id: 155,
    domain: "app-lifecycle",
    question: "A developer wants other modules to call a Server Action but, after publishing, the action does not appear when colleagues open Manage Dependencies. Aside from refreshing, what is the most likely cause?",
    options: [
      "The action returns more than one output parameter",
      "The action is a Server Action rather than a Client Action",
      "The action's Public property is still set to No in the producer",
      "The producer and consumer are in different applications",
    ],
    correctIndex: 2,
    explanation: "An element is invisible to consumers until its Public property is set to Yes in the producer. Application boundaries, output count, and server-vs-client type do not prevent a public element from appearing in Manage Dependencies.",
  },
  {
    id: 156,
    domain: "app-lifecycle",
    question: "A developer publishes a module and the operation fails with an error reported in TrueChange. What is the state of the application in the environment immediately after this failed publish?",
    options: [
      "Both old and new versions run side by side",
      "The application is taken offline until manually re-enabled",
      "The previously published working version remains running; the failed version is not deployed",
      "The new code is partially deployed and may behave inconsistently",
    ],
    correctIndex: 2,
    explanation: "A publish that fails validation/compilation does not replace the running version, so the last successfully published version keeps serving requests. OutSystems does not deploy a partial or broken build.",
  },
  {
    id: 157,
    domain: "app-lifecycle",
    question: "In a layered architecture, a developer puts shared entities in a Core module and screens in an End-User module that consumes it. Why is having the End-User module depend on the Core module (and not the reverse) preferred?",
    options: [
      "To keep a clean one-directional dependency and avoid circular references between modules",
      "Because UI modules cannot contain any logic",
      "Because only Core modules can use 1-Click Publish",
      "Because Core modules publish faster than UI modules",
    ],
    correctIndex: 0,
    explanation: "Keeping dependencies one-directional (UI depends on Core, not vice versa) prevents circular references and supports a maintainable layered architecture. The other statements are false: UI modules can hold logic and any module can be 1-Click published.",
  },
  {
    id: 158,
    domain: "app-lifecycle",
    question: "A developer needs to grant a colleague permission to deploy applications to the Production environment and to manage which users can access environments. Which tool governs these infrastructure-wide roles and permissions?",
    options: [
      "Service Studio's Manage Dependencies",
      "LifeTime",
      "The TrueChange tab",
      "Service Center on the Development environment",
    ],
    correctIndex: 1,
    explanation: "LifeTime provides centralized governance across the infrastructure, including environment access, team permissions, and deployment rights. Service Center is scoped to a single environment, and Manage Dependencies and TrueChange are design-time module features.",
  },
]
